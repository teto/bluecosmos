#ifndef FUSION_CRANDOMIZER_HPP_INCLUDED
#define FUSION_CRANDOMIZER_HPP_INCLUDED


#include <random>

namespace fus {

//namespace random {

//template<>


// see http://www.johndcook.com/cpp_TR1_random.html
// for more explanation
class CRandomGenerator {

/*
    private:
        generateLinearEngine()

        generateNormalEngine(means,varRoot);

        std::linear_congruential Engine;

        // Mersenne twister
        typedef std::mt19937 Engine;

        // Distribution
        // Mean,
        std::normal_distribution<double> dist;

        // Takes p parameter as in p/1-p
        std::bernoulli_distribution

    public:
        int getRandomInteger() const {
            std::uniform_int<int> distribution(begin, end);
            return distribution(globalEngine);
        }
*/
};

}


#endif
