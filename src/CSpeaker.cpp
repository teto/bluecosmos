#include "CSpeaker.hpp"


CSpeaker::CSpeaker(const std::wstring& wname,irr::video::ITexture* texture) :
_texture(texture)
{

    // TODO
    //if(texture == 0){}

    _name = wname;//L"Robert hue";
    //_texture =
}

