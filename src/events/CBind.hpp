/**
\file
*/
#ifndef _BC_CBINDENTRY_HPP_INCLUDED
#define _BC_CBINDENTRY_HPP_INCLUDED


#include <irrlicht.h>  // header only
//#include <fusion/core/CStringBuilder.hpp>

// Forward reference
/*
// Renommenr en CBindConfiguration / Parameters
class CBindMode
{
protected:
public:

    irr::u32 DoubleTapDelay;    //!< Might be for repat or doubletap
    ETapMode Mode;
    //bool _repeat;


    CBindMode();
    ~CBindMode();

    void setup(const ETapMode& mode,const irr::u32& delay = 10) {
        Mode = mode;
        DoubleTapDelay = delay;
//        _repeatDelay = 0;
//        _repeat = false;
    }
};
*/


#endif
